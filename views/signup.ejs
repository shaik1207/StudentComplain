<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>Sign Up | Complaint Portal</title>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
<style>
  /* (use the CSS you already have — skip pasting here to save space) */
  :root {
  --bg1: #071226;
  --bg2: #112340;
  --accent: #00d4ff;
  --card-bg: rgba(255, 255, 255, 0.06);
  --glass-border: rgba(255, 255, 255, 0.08);
  --max-width: 1200px;
}

/* reset */
* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}
html,
body {
  height: 100%;
  font-family: Inter, system-ui, Segoe UI, Roboto, Arial;
  background: linear-gradient(180deg, var(--bg1), var(--bg2));
  color: #e9f2fb;
  overflow-x: hidden;
}

/* page wrapper */
.page {
  min-height: 100vh;
  display: grid;
  place-items: center;
  padding: 24px;
}

/* frame layout */
.frame {
  width: 100%;
  max-width: var(--max-width);
  min-height: calc(100vh - 56px);
  border-radius: 16px;
  overflow: hidden;
  display: grid;
  grid-template-columns: 40% 60%;
  box-shadow: 0 30px 60px rgba(2, 8, 23, 0.6),
    inset 0 1px 0 rgba(255, 255, 255, 0.02);
  background: linear-gradient(
    180deg,
    rgba(255, 255, 255, 0.02),
    rgba(255, 255, 255, 0.01)
  );
}

/* LEFT - form area */
.left {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 48px;
  background: linear-gradient(
    180deg,
    rgba(0, 0, 0, 0.14),
    rgba(255, 255, 255, 0.02)
  );
}
.card {
  width: 100%;
  max-width: 480px;
  padding: 36px;
  border-radius: 14px;
  background: var(--card-bg);
  border: 1px solid var(--glass-border);
  backdrop-filter: blur(10px) saturate(120%);
  box-shadow: 0 12px 30px rgba(3, 10, 30, 0.5);
  transition: transform 0.28s, box-shadow 0.2s;
}
@media (min-width: 1000px) {
  .card:hover {
    transform: translateY(-6px) rotateX(2deg);
    box-shadow: 0 30px 50px rgba(0, 0, 0, 0.55);
  }
}

/* Brand section */
.brand {
  text-align: center;
  margin-bottom: 16px;
}
.brand h1 {
  color: var(--accent);
  letter-spacing: 0.6px;
  font-size: 1.7rem;
  text-shadow: 0 6px 18px rgba(0, 212, 255, 0.12);
}
.brand p {
  color: #cfefff;
  opacity: 0.85;
  font-size: 0.95rem;
  margin-top: 6px;
}

/* Form */
form {
  display: flex;
  flex-direction: column;
  gap: 14px;
  margin-top: 8px;
}
.row {
  display: flex;
  gap: 12px;
}
.col {
  flex: 1;
}
label {
  display: block;
  font-size: 0.85rem;
  color: rgba(235, 245, 255, 0.85);
  margin-bottom: 6px;
}
input[type="text"],
input[type="email"],
input[type="password"] {
  width: 100%;
  padding: 12px 14px;
  border-radius: 10px;
  border: 1px solid rgba(255, 255, 255, 0.06);
  background: linear-gradient(
    180deg,
    rgba(255, 255, 255, 0.02),
    rgba(255, 255, 255, 0.01)
  );
  color: #eaf7ff;
  font-size: 0.98rem;
  outline: none;
  transition: box-shadow 0.18s, border-color 0.18s, transform 0.12s;
}
input:focus {
  border-color: rgba(0, 212, 255, 0.9);
  box-shadow: 0 6px 20px rgba(0, 212, 255, 0.06);
  transform: translateY(-1px);
}

/* Buttons */
.actions {
  display: flex;
  gap: 12px;
  margin-top: 8px;
  flex-direction: column;
}
.btn {
  padding: 12px 14px;
  border-radius: 10px;
  border: none;
  cursor: pointer;
  font-weight: 600;
  background: linear-gradient(90deg, var(--accent), #0077d9);
  color: #032230;
  box-shadow: 0 10px 30px rgba(0, 212, 255, 0.12);
  transition: transform 0.18s, box-shadow 0.18s;
}
.btn:hover {
  transform: translateY(-4px);
  box-shadow: 0 18px 40px rgba(0, 212, 255, 0.2);
}

.muted {
  color: #bcdff1;
  font-size: 0.92rem;
  text-align: center;
  margin-top: 10px;
}

/* RIGHT - background image */
.right {
  position: relative;
  overflow: hidden;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #081027;
}
.bg {
  position: absolute;
  inset: 0;
  background-image: url("b045669d-a69c-4b04-8bbf-3bae662dd235.png");
  background-size: cover;
  background-position: center;
  transform-origin: center;
  transition: transform 10s linear;
  will-change: transform;
  filter: saturate(95%) contrast(92%);
  animation: slow-zoom 18s ease-in-out infinite;
}
@keyframes slow-zoom {
  0% {
    transform: scale(1) translateY(0);
  }
  50% {
    transform: scale(1.03) translateY(-6px);
  }
  100% {
    transform: scale(1) translateY(0);
  }
}
.overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(
    135deg,
    rgba(3, 10, 30, 0.36),
    rgba(0, 0, 0, 0.18)
  );
  mix-blend-mode: multiply;
  pointer-events: none;
}
.text-block {
  position: relative;
  z-index: 2;
  color: #dff8ff;
  text-align: left;
  max-width: 52ch;
  padding: 36px;
  transform: translateX(-10%);
  animation: textIn 1.2s ease backwards;
}
@keyframes textIn {
  from {
    transform: translateX(10%);
    opacity: 0;
  }
  to {
    transform: translateX(0);
    opacity: 1;
  }
}
.text-block h2 {
  font-size: 2rem;
  margin-bottom: 6px;
  color: #eafcff;
  text-shadow: 0 6px 30px rgba(0, 212, 255, 0.06);
}
.text-block p {
  font-size: 1.05rem;
  color: #d4eefc;
  opacity: 0.95;
}
.corner {
  position: absolute;
  right: 18px;
  bottom: 18px;
  width: 48px;
  height: 48px;
  border-radius: 12px;
  background: linear-gradient(
    180deg,
    rgba(255, 255, 255, 0.06),
    rgba(255, 255, 255, 0.02)
  );
  box-shadow: 0 6px 18px rgba(0, 0, 0, 0.4);
  display: flex;
  align-items: center;
  justify-content: center;
  color: rgba(0, 200, 255, 0.9);
  font-weight: 700;
  font-size: 18px;
  transform: rotate(18deg);
  opacity: 0.9;
}

/* Tablet and below */
@media (max-width: 1024px) {
  .frame {
    grid-template-columns: 1fr;
    min-height: auto;
    border-radius: 12px;
  }
  .left {
    order: 1;
    padding: 36px 24px;
    min-height: 60vh;
  }
  .right {
    order: 2;
    min-height: 40vh;
  }
  .text-block {
    transform: none;
    padding: 28px;
    text-align: center;
  }
  .text-block h2 {
    font-size: 1.6rem;
  }
  .bg {
    background-position: center top;
  }
}

/* Mobile view */
@media (max-width: 600px) {
  .page {
    padding: 16px;
  }
  .frame {
    grid-template-columns: 1fr;
    border-radius: 12px;
  }
  .left {
    padding: 24px 18px;
    min-height: auto;
  }
  .card {
    padding: 22px 20px;
    max-width: 100%;
  }
  .brand h1 {
    font-size: 1.25rem;
  }
  .brand p {
    font-size: 0.85rem;
  }
  .text-block {
    padding: 20px;
  }
  .text-block h2 {
    font-size: 1.3rem;
  }
  .text-block p {
    font-size: 0.95rem;
  }
  .right {
    min-height: 35vh;
  }
  .row {
    flex-direction: column;
    gap: 10px;
  }
  input {
    font-size: 0.9rem;
  }
  .btn {
    font-size: 0.9rem;
    padding: 10px 12px;
  }
}

/* Very short screens (foldables / landscape phones) */
@media (max-height: 520px) {
  .frame {
    height: auto;
  }
  .left {
    padding: 18px;
  }
  .card {
    padding: 18px;
  }
  .text-block {
    display: none; /* Hide image text for short height */
  }
}

</style>
</head>
<body>
  <div class="page">
    <div class="frame" role="main" aria-labelledby="signup-title">
      <!-- LEFT -->
      <div class="left">
        <div class="card" role="form">
          <div class="brand" id="signup-title">
            <h1>Create account</h1>
            <p>Sign up to post and manage complaints securely.</p>
          </div>

          <!-- NOTE: name="username" to match server -->
          <form action="/signup" method="post" autocomplete="on" novalidate>
            <div class="row">
              <div class="col">
                <label for="username">Full Name</label>
                <input id="username" name="username" type="text" placeholder="Your full name" required>
              </div>
            </div>

            <div>
              <label for="email">Email</label>
              <input id="email" name="email" type="email" placeholder="you@college.edu" required>
            </div>

            <div>
              <label for="reg">Register No.</label>
              <input id="reg" name="registerNo" type="text" placeholder="Register number (optional)">
            </div>

            <div class="row">
              <div class="col">
                <label for="password">Password</label>
                <input id="password" name="password" type="password" placeholder="Choose a strong password" required>
              </div>
            </div>

            <div class="actions">
              <button class="btn" type="submit">Sign Up</button>
            </div>

            <div class="muted">Already have an account? <a href="/login" style="color:var(--accent)">Login</a></div>
          </form>
        </div>
      </div>

      <!-- RIGHT (unchanged) -->
      <div class="right" aria-hidden="true">
        <div class="bg" aria-hidden="true"></div>
        <div class="overlay" aria-hidden="true"></div>

        <div class="text-block" aria-hidden="true">
          <h2>Register Your Complaint Here</h2>
          <p>Your voice matters. Report issues and help improve campus life — securely and quickly.</p>
        </div>

        <div class="corner" aria-hidden="true">★</div>
      </div>
    </div>
  </div>

<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script>
  // safe injection: message & error come from res.render
  const message = <%- JSON.stringify(message || null) %>;
  const error = <%- JSON.stringify(error || null) %>;

  if (message) {
    Swal.fire({
      toast: true,
      position: 'top-end',
      icon: message.type || 'success',
      title: message.text || 'Success!',
      showConfirmButton: false,
      timer: 2500,
      timerProgressBar: true,
    });
  }

  if (error) {
    Swal.fire({
      icon: 'error',
      title: 'Error',
      text: error,
      confirmButtonColor: '#007bff',
    });
  }
</script>

</body>
</html>
